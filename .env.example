# Application login
APP_USERNAME=admin
# For development only; in production set a strong password and hash storage
APP_PASSWORD=changeme

# Optional: long-lived API key default expiry days
API_KEY_EXPIRY_DAYS=365

# MirAIe provider credentials (server-side only)
MIRAIE_USERNAME=
MIRAIE_PASSWORD=
