version: '3.8'
services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: miraie
      POSTGRES_PASSWORD: miraie
      POSTGRES_DB: my_home
    # Avoid binding to host 5432 if the host already runs Postgres.
    # Map to 5433 on the host to prevent conflicts; change or remove if you prefer.
    ports:
      - 5433:5432
    volumes:
      - db_data:/var/lib/postgresql/data

  app:
    build: .
    ports:
      - 3000:3000
    environment:
      DATABASE_URL: postgres://miraie:miraie@db:5432/my_home
    env_file:
      - .env
    depends_on:
      - db

volumes:
  db_data:
